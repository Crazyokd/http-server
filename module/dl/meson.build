dl1 = library('dl1', 'dl1.c',
    include_directories : [comm_inc],
    install_dir : libdir,
    install : true)

dl2 = library('dl2', 'dl2.cpp',
    include_directories : [comm_inc],
    install_dir : libdir,
    install : true)